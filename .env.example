# Скопируется в apps/api/.env скриптом npm run setup (если файла ещё нет).

# Docker: см. docker-compose.yml — Postgres на хосте на порту 5433
DATABASE_URL=postgresql://logix:logix@localhost:5433/logix?schema=public

PORT=3000

# WEB_ORIGIN=http://localhost:5173

# Dadata: подсказки адресов (КЛАДР/ФИАС). Ключ — в apps/api/.env
# DADATA_API_KEY=
